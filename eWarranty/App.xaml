<?xml version="1.0" encoding="utf-8"?>
<Application xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="eWarranty.App" >
    <Application.Resources>
      <!--<ResourceDictionary Source="Styles/Global.xaml"/>-->
        <!--<ResourceDictionary Source="~/Styles/RadioTemplates.xaml"/>-->
        <Color x:Key="NavigationPrimary">#5aa9e6</Color>
        <Color x:Key="TransparentColor">Transparent</Color>
        <Color x:Key="BottomTab">White</Color>
        <Color x:Key="WhiteColor">White</Color>
        <Color x:Key="inputTitle">Black</Color>
        <Color x:Key="BlackColor">Black</Color>
        <Color x:Key="YellowColor">#ffe45e</Color>
        <Color x:Key="contentBG">#F5F5F5</Color>
        <Color x:Key="inputPlaceholder">#687089</Color>
        <Color x:Key="inputText">Black</Color>        
        <Color x:Key="lblTitle">#687089</Color>
        <Color x:Key="separatorline">#687089</Color>
        <Color x:Key="backgroungColor">#00A2BF</Color>        
        <Color x:Key="BlueColor">#6c1e36</Color>
        <!--<Color x:Key="BlueColor">#75428E</Color> #6c1e36-->
        <Color x:Key="GreenColor">#008000</Color>
        <Color x:Key="RedColor">#FF0000</Color>
        <Color x:Key="GrayColor">#919191</Color>
        <Color x:Key="PinkColor">#EA1D24</Color>
        <Color x:Key="borderColor">#ECECEC</Color>     
        <Color x:Key="LightGray">LightGray</Color>
        <Color x:Key="DarkGray">#3A3A3A</Color>
        <Color x:Key="btnbgColor">#EA1D24</Color>
        <Color x:Key="LoginBg">#eb7b5b</Color>
        <Color x:Key="DarkCyanBg">#02d2ed</Color>
        <Color x:Key="LabelGrayShade">#959493</Color>
        <Color x:Key="LighterGray">#F2F3F4</Color>
        <Color x:Key="PurpleColor">#7b39e0</Color>
        <Color x:Key="SkyBlueolor">#0099FF</Color>
        <Color x:Key="DarkBlueolor">#1867b2</Color>
        <Color x:Key="Color1">#1E55A5</Color>
        <Color x:Key="Color2">#EA1D24</Color>
        <Color x:Key="Color3">#96C9F4</Color>
        <Color x:Key="Color4">#80f9b7</Color>
        <Color x:Key="GrayShade">#515151</Color>
        <Color x:Key="PlcGray">#C1C1C1</Color>
        <Color x:Key="Pinkshade1">#e7d6e6</Color>
        <!--<Color x:Key="Pinkshade1">#DBCCE3</Color> #e7d6e6-->
        <Color x:Key="Pinkshade">#b6ccfe</Color>
        <Color x:Key="YellowColorShade">#e9edc9</Color>
        <Color x:Key="YellowColorShade1">#EDF7F3</Color>
        <Color x:Key="YellowColorShade2">#FFFAE5</Color>
        <Color x:Key="YellowColorShade3">#ffdc5e</Color>
        <Color x:Key="Blueshade">#E5FFF1</Color>
        <Color x:Key="Brownshade">#7e5e69</Color>
        <Color x:Key="BlueColorhade">#E8F3FD</Color>
        <Color x:Key="BlueColorShade1">#0074d3</Color>

        

         <ResourceDictionary>          
            <OnPlatform x:TypeArguments="x:String" x:Key="FontAwesomeSolid" Android="FontAwesome5FreeSolid900.otf#Regular" iOS="FontAwesome5Free-Solid" />
        </ResourceDictionary>

        <OnPlatform x:Key="grideborderColor"
            x:TypeArguments="Color"
            iOS="Transparent"
            Android="Gray" />
         
            <Style TargetType="NavigationPage">
                <Setter Property="BarBackgroundColor">
                    <Setter.Value>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="{StaticResource BlueColor}" Offset="1.0"/>
                            <GradientStop Color="{StaticResource BlueColor}" Offset="1.0"/>
                        </LinearGradientBrush>
                    </Setter.Value>
                </Setter>

                 <!--<Setter Property="BarBackground">
                    <LinearGradientBrush StartPoint="0,0"
                                         EndPoint="1,1">
                      <GradientStop Color="#26C986"
                                    Offset="0.25" />
                      <GradientStop Color="#109F8D"
                                    Offset="0.75" />             
                      <GradientStop Color="#36ED81"
                                    Offset="1.0" />
                   </LinearGradientBrush>   
                </Setter>-->
                
                <Setter Property="BarTextColor" Value="White" />
            </Style>
        <OnPlatform x:Key="FontFamilyName" x:TypeArguments="x:String" iOS="segoeui" Android="segoeui" />
        <OnPlatform x:Key="semiFontFamilyName" x:TypeArguments="x:String" iOS="segoeuibold" Android="segoeuibold" />
        
         <Style x:Key="DescLabel" TargetType="Label" >
             <Setter Property="FontAttributes" Value="Bold"/>
          <Setter Property="FontFamily" Value="FontName"/>
           <Setter Property="FontSize" Value="17"/>
           <Setter Property="TextColor" Value="#808080"/>
       </Style>

         <Style x:Key="EnteryLabelValues" TargetType="Label" >
            <Setter Property="FontFamily" Value="calibribold"/>
            <Setter Property="FontSize" Value="{OnPlatform iOS='15',Android='14'}"/>
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="TextColor" Value="{StaticResource GrayShade}"/>
            <Setter Property="Opacity" Value="100"/>
       </Style>
        
        <Style TargetType="Label">
                <Setter Property="FontFamily" Value="segoeui"/>
             
        </Style>
        <Style TargetType="Button" x:Key="ButtonPinkStyle">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush>
                        <GradientStop Color="{StaticResource Pinkshade1}" Offset="0.4"/>
                        <GradientStop Color="{StaticResource Pinkshade1}" Offset="1.0"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="FontFamily" Value="calibribold"/>
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="TextColor" Value="{StaticResource BlackColor}" />
            <Setter Property="FontSize" Value="{OnPlatform iOS='16',Android='15'}" />
            <Setter Property="TextTransform" Value="None"/>
            <Setter Property="HeightRequest" Value="42"/>
            <Setter Property="CornerRadius" Value="15"/>
            <Setter Property="BorderColor" Value="{StaticResource Pinkshade1}"/>
            <Setter Property="Opacity" Value="20"/>
            <Setter Property="BorderWidth" Value="2"/>

        </Style>

        <Style TargetType="Button" x:Key="ButtonBrickRedStyle">

            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush>
                       <GradientStop Color="{StaticResource WhiteColor}" Offset="0.4"/>
                       <GradientStop Color="{StaticResource WhiteColor}" Offset="1.0"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="FontFamily" Value="calibribold"/>
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="TextColor" Value="{StaticResource BlueColorShade1}" />
            <Setter Property="FontSize" Value="{OnPlatform iOS='16',Android='15'}" />
            <Setter Property="TextTransform" Value="None"/>
            <Setter Property="HeightRequest" Value="42"/>
            <Setter Property="CornerRadius" Value="15"/>
            <Setter Property="BorderColor" Value="{StaticResource BlueColor}"/>
            <Setter Property="BorderWidth" Value="2"/>
            <Setter Property="Opacity" Value="20"/>

        </Style>

        <Style TargetType="Button" x:Key="ButtonPinkShadeStyle">

            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush>
                       <GradientStop Color="{StaticResource WhiteColor}" Offset="0.4"/>
                        <GradientStop Color="{StaticResource WhiteColor}" Offset="1.0"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="FontFamily" Value="calibribold"/>
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="TextColor" Value="{StaticResource BlueColorShade1}" />
            <Setter Property="FontSize" Value="{OnPlatform iOS='16',Android='15'}" />
            <Setter Property="TextTransform" Value="None"/>
            <Setter Property="HeightRequest" Value="42"/>
            <Setter Property="CornerRadius" Value="15"/>
            <Setter Property="BorderColor" Value="{StaticResource BlueColor}"/>
            <Setter Property="BorderWidth" Value="2"/>
            <Setter Property="Opacity" Value="20"/>

        </Style>

        <Style TargetType="Button" x:Key="ButtonBlueStyle">

            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush>
                       <GradientStop Color="{StaticResource BlueColor}" Offset="0.4"/>
                        <GradientStop Color="{StaticResource BlueColor}" Offset="1.0"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="FontFamily" Value="calibribold"/>
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="TextColor" Value="{StaticResource WhiteColor}" />
            <Setter Property="FontSize" Value="{OnPlatform iOS='16',Android='15'}" />
            <Setter Property="TextTransform" Value="None"/>
            <Setter Property="HeightRequest" Value="42"/>
            <Setter Property="CornerRadius" Value="15"/>
            <Setter Property="BorderColor" Value="{StaticResource BlueColor}"/>
            <Setter Property="BorderWidth" Value="2"/>
            <Setter Property="Opacity" Value="20"/>

        </Style>

         <Style TargetType="SearchBar" x:Key="SearchStyle">
            <Setter Property="BackgroundColor" Value="{StaticResource WhiteColor}"/>
            <Setter Property="CancelButtonColor" Value="{StaticResource GrayColor}"/>
            <Setter Property="FontFamily" Value="calibribold"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="TextColor" Value="{StaticResource GrayShade}"/>
            <Setter Property="PlaceholderColor" Value="{StaticResource PlcGray}"/>
            <Setter Property="Opacity" Value="100"/>
            
        </Style>

        <Style TargetType="Label"
               Class="FlyoutItemLabelStyle">
            <Setter Property="TextColor"
                    Value="Black" />
            <Setter Property="HeightRequest"
                    Value="100" />
        </Style>

        <Style TargetType="Frame" x:Key="FrameStyle">
            <Setter Property="HasShadow" Value="{OnPlatform Android='True' ,iOS='False'}"/>
            <Setter Property="CornerRadius" Value="20"/>
            <Setter Property="IsClippedToBounds" Value="True"/>
            <Setter Property="BorderColor" Value="AliceBlue"/>

        </Style>

         <Style TargetType="BoxView" x:Key="CardBlueStyle">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="{StaticResource Pinkshade1}" Offset="0.1"/>
                        <GradientStop Color="{StaticResource Pinkshade1}" Offset="1.0"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="HeightRequest" Value="100"/>
            <Setter Property="CornerRadius" Value="0,0,10,300"/>
        </Style>

         <Style x:Key="EnteryTitleLabel" TargetType="Label">
            <Setter Property="FontFamily" Value="calibribold"/>
            <Setter Property="FontSize" Value="{OnPlatform iOS='16',Android='15'}"/>
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="TextColor" Value="{StaticResource BlackColor}"/>
            <Setter Property="Opacity" Value="100"/>
        </Style>

        <Style x:Key="ChartLabel" TargetType="Label">
            <Setter Property="FontFamily" Value="calibribold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="TextColor" Value="{StaticResource BlackColor}"/>
        </Style>

        <Style x:Key="MyTitleLabel" TargetType="Label">
            <Setter Property="FontFamily" Value="calibribold"/>
            <Setter Property="FontSize" Value="{OnPlatform iOS='16',Android='15'}"/>
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="TextColor" Value="{StaticResource BlackColor}"/>
            <Setter Property="Opacity" Value="100"/>
        </Style>

        <Style x:Key="MenuTitleLabel" TargetType="Label">
            <Setter Property="FontFamily" Value="LeagueSpartan-SemiBold"/>
            <Setter Property="FontSize" Value="17"/>
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="TextColor" Value="{StaticResource BlackColor}"/>
            <Setter Property="Opacity" Value="100"/>
        </Style>

        <Style x:Key="MyValueLabel" TargetType="Label">
            <Setter Property="FontFamily" Value="calibribold"/>
            <Setter Property="FontSize" Value="{OnPlatform iOS='15',Android='14'}"/>
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="TextColor" Value="{StaticResource GrayShade}"/>
            <Setter Property="Opacity" Value="100"/>
        </Style>

        <Style x:Key="TimePickerStyle" TargetType="TimePicker">
            <Setter Property="FontFamily" Value="calibribold"/>
            <Setter Property="FontSize" Value="{OnPlatform iOS='15',Android='14'}"/>
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="TextColor" Value="{StaticResource GrayShade}"/>
            <Setter Property="Opacity" Value="100"/>
        </Style>

        <Style x:Key="EnteryTxtValues" TargetType="Entry">
            <Setter Property="FontFamily" Value="calibribold"/>
            <Setter Property="FontSize" Value="{OnPlatform iOS='15',Android='14'}"/>
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="TextColor" Value="{StaticResource GrayShade}"/>
            <Setter Property="PlaceholderColor" Value="{StaticResource PlcGray}"/>
            <Setter Property="Opacity" Value="100"/>
        </Style>

        <Style x:Key="EditorTxtValues" TargetType="Editor">
            <Setter Property="FontFamily" Value="calibribold"/>
            <Setter Property="FontSize" Value="{OnPlatform iOS='16',Android='15'}"/>
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="TextColor" Value="{StaticResource GrayShade}"/>
            <Setter Property="PlaceholderColor" Value="{StaticResource PlcGray}"/>
            <Setter Property="Opacity" Value="100"/>
        </Style>

        <Style TargetType="Label" x:Key="DashboardButtonLabel">
            <Setter Property="TextColor" Value="{StaticResource BlueColor}"/>
            <Setter Property="FontSize" Value="{OnPlatform iOS='16',Android='15'}"/>
            <Setter Property="FontFamily" Value="calibribold"/>
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="Opacity" Value="100"/>
        </Style>

        <Style TargetType="Label" x:Key="LabelBlue">
            <Setter Property="TextColor" Value="{StaticResource BlueColor}"/>
            <Setter Property="FontSize" Value="{OnPlatform iOS='17',Android='16'}"/>
            <Setter Property="FontFamily" Value="calibribold"/>
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="Opacity" Value="100"/>
        </Style>

        <Style TargetType="Label" x:Key="LabelLargeBlue">
            <Setter Property="TextColor" Value="{StaticResource BlueColor}"/>
            <Setter Property="FontSize" Value="{OnPlatform iOS='20',Android='18'}"/>
            <Setter Property="FontFamily" Value="calibribold"/>
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="Opacity" Value="100"/>
        </Style>

        <Style TargetType="Label" x:Key="SimpleLabelBlack">
            <Setter Property="TextColor" Value="{StaticResource BlackColor}"/>
            <Setter Property="FontSize" Value="{OnPlatform iOS='16',Android='15'}"/>
            <Setter Property="FontFamily" Value="calibribold"/>
            <Setter Property="Opacity" Value="100"/>
        </Style>

        <Style TargetType="Label" x:Key="LabelLargeBlack">
            <Setter Property="TextColor" Value="{StaticResource BlackColor}"/>
            <Setter Property="FontSize" Value="{OnPlatform iOS='20',Android='18'}"/>
            <Setter Property="FontFamily" Value="calibribold"/>
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="Opacity" Value="100"/>
        </Style>

        <Style TargetType="Label" x:Key="LabelLargeGreen">
            <Setter Property="TextColor" Value="{StaticResource GreenColor}"/>
            <Setter Property="FontSize" Value="{OnPlatform iOS='20',Android='18'}"/>
            <Setter Property="FontFamily" Value="calibribold"/>
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="Opacity" Value="100"/>
        </Style>

        <Style TargetType="Label" x:Key="LabelLargeWhite">
            <Setter Property="TextColor" Value="{StaticResource WhiteColor}"/>
            <Setter Property="FontSize" Value="{OnPlatform iOS='20',Android='18'}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="Opacity" Value="100"/>
        </Style>

        <Style TargetType="Label" x:Key="LabelLargeGray">
            <Setter Property="TextColor" Value="{StaticResource GrayColor}"/>
            <Setter Property="FontSize" Value="{OnPlatform iOS='20',Android='18'}"/>
            <Setter Property="FontFamily" Value="calibribold"/>
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="Opacity" Value="100"/>
        </Style>

        <Style TargetType="Label" x:Key="LabelLargeRed">
            <Setter Property="TextColor" Value="{StaticResource RedColor}"/>
            <Setter Property="FontSize" Value="{OnPlatform iOS='20',Android='18'}"/>
            <Setter Property="FontFamily" Value="calibribold"/>
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="Opacity" Value="100"/>
        </Style>

        <Style TargetType="Label" x:Key="LabelMediumWhite">
            <Setter Property="TextColor" Value="{StaticResource WhiteColor}"/>
            <Setter Property="FontSize" Value="{OnPlatform iOS='14',Android='13'}"/>
            <Setter Property="FontFamily" Value="calibribold"/>
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="Opacity" Value="100"/>
        </Style>

        <Style TargetType="Label" x:Key="LabelMediumBlack">
            <Setter Property="TextColor" Value="{StaticResource BlackColor}"/>
            <Setter Property="FontSize" Value="{OnPlatform iOS='14',Android='13'}"/>
            <Setter Property="FontFamily" Value="calibribold"/>
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="Opacity" Value="100"/>
        </Style>

        <Style TargetType="Label" x:Key="LabelMediumYellow">
            <Setter Property="TextColor" Value="{StaticResource LightGray}"/>
            <Setter Property="BackgroundColor" Value="{StaticResource YellowColor}" />
            <Setter Property="FontSize" Value="{OnPlatform iOS='14',Android='13'}"/>
            <Setter Property="FontFamily" Value="calibribold"/>
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="Opacity" Value="100"/>
        </Style>

        <Style TargetType="Label" x:Key="LabelMediumGreen">
            <Setter Property="TextColor" Value="{StaticResource GreenColor}"/>
            <Setter Property="FontSize" Value="{OnPlatform iOS='14',Android='13'}"/>
            <Setter Property="FontFamily" Value="calibribold"/>
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="Opacity" Value="100"/>
        </Style>

        <Style TargetType="Label" x:Key="LabelMediumRed">
            <Setter Property="TextColor" Value="{StaticResource RedColor}"/>
            <Setter Property="FontSize" Value="{OnPlatform iOS='14',Android='13'}"/>
            <Setter Property="FontFamily" Value="calibribold"/>
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="Opacity" Value="100"/>
        </Style>

        <Style TargetType="Label" x:Key="LabelMediumBlue">
            <Setter Property="TextColor" Value="{StaticResource BlueColor}"/>
            <Setter Property="FontSize" Value="{OnPlatform iOS='14',Android='13'}"/>
            <Setter Property="FontFamily" Value="calibribold"/>
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="Opacity" Value="100"/>
        </Style>

        <Style TargetType="Label" x:Key="LabelMediumGray">
            <Setter Property="TextColor" Value="{StaticResource GrayColor}"/>
            <Setter Property="FontSize" Value="{OnPlatform iOS='16',Android='15'}"/>
            <Setter Property="FontFamily" Value="calibribold"/>
            <Setter Property="Opacity" Value="100"/>
        </Style>

        <Style TargetType="Label" x:Key="LabelGray">
            <Setter Property="TextColor" Value="{StaticResource DarkGray}"/>
            <Setter Property="FontSize" Value="{OnPlatform iOS='14',Android='13'}"/>
            <Setter Property="FontFamily" Value="calibribold"/>
            <Setter Property="Opacity" Value="100"/>
        </Style>

        <Style TargetType="Label" x:Key="Placeholder">
            <Setter Property="TextColor" Value="{StaticResource GrayColor}"/>
            <Setter Property="FontSize" Value="{OnPlatform iOS='14',Android='13'}"/>
            <Setter Property="FontFamily" Value="calibribold"/>
            <Setter Property="Opacity" Value="100"/>
        </Style>


        <Style TargetType="Label" x:Key="LabelSmallWhite">
            <Setter Property="TextColor" Value="{StaticResource WhiteColor}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="calibribold"/>
            <Setter Property="Opacity" Value="100"/>
        </Style>

        <Style TargetType="Label" x:Key="LabelSmallBlack">
            <Setter Property="TextColor" Value="{StaticResource BlackColor}"/>
            <Setter Property="FontSize" Value="{OnPlatform iOS='14',Android='13'}"/>
            <Setter Property="FontFamily" Value="calibribold"/>
            <Setter Property="Opacity" Value="100"/>
        </Style>

        <Style TargetType="Label" x:Key="LabelSmallRed">
            <Setter Property="TextColor" Value="{StaticResource RedColor}"/>
            <Setter Property="FontSize" Value="{OnPlatform iOS='14',Android='13'}"/>
            <Setter Property="FontFamily" Value="calibribold"/>
            <Setter Property="Opacity" Value="100"/>
        </Style>

        <Style TargetType="Label" x:Key="LabelSmallGreen">
            <Setter Property="TextColor" Value="{StaticResource GreenColor}"/>
            <Setter Property="FontSize" Value="{OnPlatform iOS='14',Android='13'}"/>
            <Setter Property="FontFamily" Value="calibribold"/>
            <Setter Property="Opacity" Value="100"/>
        </Style>

        <Style TargetType="Label" x:Key="LabelSmallBlue">
            <Setter Property="TextColor" Value="{StaticResource BlueColor}"/>
            <Setter Property="FontSize" Value="{OnPlatform iOS='14',Android='13'}"/>
            <Setter Property="FontFamily" Value="calibribold"/>
            <Setter Property="Opacity" Value="100"/>
        </Style>

        <Style TargetType="Label" x:Key="LabelSmallGray">
            <Setter Property="TextColor" Value="{StaticResource DarkGray}"/>
            <Setter Property="FontSize" Value="{OnPlatform iOS='14',Android='13'}"/>
            <Setter Property="FontFamily" Value="calibribold"/>
            <Setter Property="Opacity" Value="100"/>
        </Style>

        <Style TargetType="Label" x:Key="LabelLeftSideMenu">
            <Setter Property="TextColor" Value="{StaticResource BlackColor}"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontFamily" Value="calibribold"/>
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="Opacity" Value="100"/>
        </Style>

    </Application.Resources>
</Application>